package com.nt.audax;

/* Functional Interface :- If an Interface contains only one abstract method then it called a functional interface. Example Runnable, Callable, Comparable etc.
 * To call lambda we need functional interface.
 * Lambda is used to implement functional interface in a very simple and short manner.
 */

public interface PracticeLambda 
{
	String joinString(String a, String b);
}
